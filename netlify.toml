[build]
  command   = ""
  publish   = "."

[build.environment]
  NODE_VERSION = "18"

# Netlify Identity — necessário para o Decap CMS funcionar
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-XSS-Protection = "1; mode=block"
